# brain/wiki/_metadata.yml
freeze: auto

# Force Quarto to completely nullify the broken key for HTML generation
metadata:
  sources: null

format:
  html:
    toc: true
    filters:
      - ./wikilinks.lua