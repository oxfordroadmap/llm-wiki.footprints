# brain/wiki/_metadata.yml
freeze: auto

# This forces Quarto to bypass trying to read 'sources' as an active array mapping object
metadata-files: [] 

format:
  html:
    toc: true
    filters:
      - ./wikilinks.lua